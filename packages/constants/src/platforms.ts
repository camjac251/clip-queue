/**
 * Platform Constants
 * Re-export from @cq/platforms for convenience
 */

export enum Platform {
  KICK = 'kick',
  SORA = 'sora',
  TWITCH = 'twitch'
}

export type PlatformValue = `${Platform}`
