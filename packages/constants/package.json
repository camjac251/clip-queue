{
  "name": "@cq/constants",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts",
    "./events": "./src/events.ts",
    "./commands": "./src/commands.ts",
    "./platforms": "./src/platforms.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "lint": "prettier --check . --log-level=warn && eslint . --quiet --cache",
    "lint:fix": "eslint . --fix --cache"
  },
  "devDependencies": {
    "@cq/config": "workspace:*",
    "typescript": "catalog:"
  }
}
